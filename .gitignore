# Local Netlify folder
.netlify

# Node
node_modules
package-lock.json

# OS / editor
.DS_Store
Thumbs.db
.vscode
.idea

# Private address geocache (contains full addresses — PII, never deploy)
# Lives at ~/.visionbleu-cache/ (outside project tree), this entry is just
# belt-and-suspenders in case someone copies it in.
scripts/.cache/
.visionbleu-cache/

# Raw video sources (converted to assets/video/showcase.mp4 — never commit the heavy original)
assets/video/_src*
